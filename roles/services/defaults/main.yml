---
services: []

container_default_opts:
  user: "{{ PUID }}:{{ PGID }}"
  pull: true
  state: started 
  restart_policy: unless-stopped
  command_handling: correct
  env: {}
  ports: []
  command: []
  volumes: []
  labels: {}